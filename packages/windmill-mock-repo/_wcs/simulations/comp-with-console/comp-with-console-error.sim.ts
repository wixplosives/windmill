import { createSimulation } from '@wixc3/wcs-core';
import { CompWithConsoleError } from '../../../src';

export default createSimulation({
    name: 'CompWithConsoleError',
    componentType: CompWithConsoleError,
    props: {},
});
