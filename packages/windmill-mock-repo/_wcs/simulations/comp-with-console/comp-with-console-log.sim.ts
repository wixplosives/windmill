import { createSimulation } from '@wixc3/wcs-core';
import { CompWithConsoleLog } from '../../../src';

export default createSimulation({
    name: 'CompWithConsoleLog',
    componentType: CompWithConsoleLog,
    props: {},
});
