import { createSimulation } from '@wixc3/wcs-core';
import { CompWithHook } from '../../../src';

export default createSimulation({
    name: 'CompWithHook',
    componentType: CompWithHook,
    props: {},
});
