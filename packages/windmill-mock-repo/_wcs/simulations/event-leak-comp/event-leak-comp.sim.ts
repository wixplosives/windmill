import { createSimulation } from '@wixc3/wcs-core';
import { EventLeakComp } from '../../../src';

export default createSimulation({
    name: 'EventLeakComp',
    componentType: EventLeakComp,
    props: {},
});
