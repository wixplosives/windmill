{
  "name": "@windmill/sanity",
  "version": "1.0.0",
  "main": "cjs/index.js",
  "module": "esm/index.js",
  "types": "esm/index.d.ts",
  "bin": {
    "windmill-sanity": "bin/cli.js"
  },
  "scripts": {
    "clean": "rimraf ./cjs ./esm",
    "prebuild": "yarn clean",
    "build": "yarn build:cjs && yarn build:esm",
    "build:watch": "run-p build:cjs-watch build:esm-watch",
    "build:cjs-watch": "tsc -p tsconfig.build.json --watch --outDir cjs --module commonjs",
    "build:esm-watch": "tsc -p tsconfig.build.json --watch --outDir esm --module esnext",
    "build:cjs": "tsc -p tsconfig.build.json --module commonjs --outDir cjs",
    "build:esm": "tsc -p tsconfig.build.json --module esnext --outDir esm",
    "test": "mocha -r @ts-tools/node/r -r @stylable/node/register \"test/**/*.unit.ts?(x)\"",
    "prepack": "yarn build"
  },
  "peerDependencies": {
    "react": ">=16"
  },
  "files": [
    "cjs",
    "esm",
    "src",
    "bin",
    "templates"
  ],
  "license": "MIT",
  "repository": "git@github.com:wixplosives/windmill.git",
  "sideEffects": false,
  "private": true,
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@file-services/memory": "^2.2.0",
    "sinon": "^9.0.1",
    "sinon-chai": "^3.5.0",
    "temp": "^0.9.1",
    "webpack-virtual-modules": "^0.2.1"
  }
}
