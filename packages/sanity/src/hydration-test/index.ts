import { hydrationTest } from './hydration-test';
// import {eventListenerTest} from './event-listener-test';

// eslint-disable-next-line no-console
hydrationTest().catch((err) => console.error(err));
// eventListenerTest();
